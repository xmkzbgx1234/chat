下面给你一版**适合校招 C++ 后端的 RPC 项目方案**，目标是：**真实可做、2～4 周能落地、技术点足够支撑简历和面试**。

项目不要追求“大而全”，而要追求这三点：

1. **能跑起来**
2. **能讲清楚设计**
3. **能做出一点优化和压测结果**

---

# 项目定位

**项目名：基于 C++/Protobuf 的轻量级 RPC 框架**

适合写在简历上的一句话：

> 基于 C++、TCP/epoll 和 Protobuf 实现轻量级 RPC 框架，支持服务发布、远程调用、请求路由、超时控制与简单服务发现。

这个项目和你现有的聊天服务器很搭：

* 聊天服务器体现你会做**业务服务**
* RPC 项目体现你会做**通用服务端基础设施**

这样简历主线会很完整。

---

# 一、项目最终要做到什么程度

你不需要上来就做成 “Dubbo / brpc / grpc”。
校招项目做到下面这个层级已经够用了：

### 最低可行能力

* 服务端能发布服务
* 客户端能像本地函数一样发起远程调用
* 用 Protobuf 定义接口和消息
* 基于 TCP 完成请求/响应通信
* 解决粘包拆包
* 服务端支持并发处理
* 有日志、配置、错误码
* 能做简单压测

### 加分能力

* epoll/Reactor
* 线程池
* 服务注册与发现
* 超时控制
* 连接池/长连接
* 简单负载均衡
* benchmark 数据

---

# 二、推荐的项目架构

建议你拆成这 6 个模块。

## 1. 公共模块 common

放公共工具：

* 日志模块
* 配置读取
* 状态码/错误码
* 工具函数
* 时间统计工具

### 你能学到

* 模块化设计
* 工程目录结构
* 公共组件封装

---

## 2. 协议模块 protocol

负责定义 RPC 通信协议。

### 建议协议格式

你可以设计成：

* `magic`：协议标识
* `version`：版本号
* `header_len`：头部长度
* `body_len`：消息体长度
* `request_id`：请求唯一标识
* `service_name`
* `method_name`
* `error_code`
* `payload`：protobuf 序列化后的内容

### 为什么要这么设计

因为面试官会问你：

* 怎么区分一个完整请求
* 怎么解决 TCP 粘包拆包
* 怎么知道调的是哪个服务哪个方法
* 怎么做响应匹配

这个模块是很重要的亮点。

---

## 3. 网络模块 network

负责底层通信。

### 第一版建议

先别一上来就搞太复杂。
你可以分两阶段：

#### 阶段 A：基础版

* Linux socket
* TCP 通信
* 阻塞 IO 或简单多线程模型

先把 RPC 跑通。

#### 阶段 B：增强版

* epoll
* 非阻塞 socket
* 事件循环
* Reactor 思想

这样项目推进更稳。

### 这个模块包含

* TcpServer
* TcpConnection
* Buffer
* EventLoop（增强版）
* Acceptor（增强版）

---

## 4. 服务端模块 provider

负责服务发布和请求处理。

### 要做的事情

* 注册服务对象
* 保存 service 和 method 映射关系
* 收到请求后解析：

  * 识别 service_name
  * 识别 method_name
  * 反序列化请求
  * 调用对应方法
  * 序列化响应返回

### 核心数据结构

比如：

* `unordered_map<string, ServiceInfo>`
* `ServiceInfo` 里维护 method 映射

### 面试高频点

* 请求怎么分发到对应方法
* 为什么要用字符串映射
* protobuf service 怎么组织
* 服务注册流程是什么

---

## 5. 客户端模块 consumer

负责发起远程调用。

### 功能

* 封装 RPC 调用接口
* 根据服务名找到服务地址
* 建立连接
* 序列化请求并发送
* 接收响应并反序列化
* 返回调用结果

### 可做成什么样

理想状态下让用户代码看起来像这样：

```cpp
UserServiceRpc_Stub stub(channel);
LoginRequest req;
LoginResponse resp;
stub.Login(&req, &resp);
```

你不一定要完全复刻 gRPC 的风格，但要尽量做到“**调用方感觉像本地函数调用**”。

---

## 6. 注册发现模块 registry

这个模块别做太重。

### 校招最推荐方案

做一个**轻量版注册中心**，别碰 ZooKeeper 全家桶。

你可以选下面任意一个：

#### 方案 A：本地配置中心

* 服务启动时读取配置
* 客户端从配置文件获取地址

最简单，但技术亮点少。

#### 方案 B：基于 Redis 的注册发现

* 服务启动时把 `service_name -> ip:port` 写入 Redis
* 客户端调用前到 Redis 查询地址

这个方案最适合你，因为你已经有 Redis 项目经验。

#### 方案 C：自己做一个轻量注册中心

* registry server 维护服务列表
* provider 启动时注册
* consumer 调用前查询

这个最完整，但工作量更大。

### 我的建议

**优先选 B：Redis 注册发现。**

因为：

* 实现成本低
* 和你现有技术栈一致
* 简历上很好写
* 面试也能展开讲

---

# 三、推荐技术栈

按“必须有”和“加分项”分。

## 必须有

* C++11/14/17
* Linux
* Socket
* Protobuf
* 多线程
* CMake
* gdb
* valgrind
* 日志模块

## 强烈建议有

* epoll
* 线程池
* Redis
* 自定义协议
* 压测工具

## 可选加分

* 长连接复用
* 超时定时器
* 负载均衡
* 心跳检测
* 重试机制

---

# 四、开发顺序

按这个顺序做，最稳。

## 第 1 阶段：先跑通最小 RPC 闭环

目标：先完成一次远程调用。

你要做：

* protobuf 定义请求/响应
* TCP client/server
* 自定义协议头
* 客户端发送请求
* 服务端解析并调用方法
* 服务端返回响应

### 结果

先实现一个最简单的例子：

* `UserService.Login(username, password)`

这一步成功了，项目就有骨架了。

---

## 第 2 阶段：服务发布与动态分发

目标：不是只支持一个函数，而是支持多个 service/method。

你要做：

* 服务注册接口
* method 映射表
* 通用请求分发器
* 统一响应封装

### 结果

支持：

* UserService.Login
* UserService.Register
* FriendService.GetFriendList

---

## 第 3 阶段：并发模型

目标：让服务端能处理多个请求。

### 推荐做法

* 主线程负责 accept
* 工作线程池负责业务处理

或者：

* epoll 负责网络事件
* 线程池处理业务逻辑

### 这一步要讲清楚

* 为什么要线程池
* 为什么不能所有事都在主线程做
* IO 和业务处理怎么拆分

---

## 第 4 阶段：注册与发现

目标：调用方不写死地址。

### 基于 Redis 实现

* provider 启动时注册服务
* consumer 发起调用前查询服务地址
* 支持同一服务多个实例

### 可以顺便做一个简单负载均衡

* 随机
* 轮询

这个已经足够当加分点了。

---

## 第 5 阶段：鲁棒性增强

目标：别只在理想路径能跑。

你要补：

* 请求超时
* 服务不存在
* 方法不存在
* 连接失败
* 解码失败
* protobuf 反序列化失败
* 日志输出
* 错误码返回

---

## 第 6 阶段：压测与优化

目标：给简历加“结果”。

### 你可以测什么

* 单连接连续调用耗时
* 多线程并发调用 QPS
* 平均响应时间
* 99 线延迟
* 线程池大小对吞吐的影响

### 工具

* 自己写 benchmark client 就够了
* 不用强行上 wrk，因为 wrk 更适合 HTTP

---

# 五、建议你做的 Demo 服务

为了让项目更像真实框架，建议至少做 2 个业务服务。

## 服务 1：UserService

接口：

* `Login`
* `Register`
* `GetUserInfo`

## 服务 2：FriendService

接口：

* `GetFriendList`
* `AddFriend`

这样面试时你可以证明：

* 不是只写死了一个 demo
* 框架确实支持多服务、多方法调用

---

# 六、建议目录结构

你可以参考这种组织方式：

```text
rpc-project/
├── bin/
├── build/
├── cmake/
├── conf/
│   └── rpc.conf
├── include/
│   ├── common/
│   ├── network/
│   ├── protocol/
│   ├── provider/
│   ├── consumer/
│   └── registry/
├── src/
│   ├── common/
│   ├── network/
│   ├── protocol/
│   ├── provider/
│   ├── consumer/
│   └── registry/
├── proto/
│   ├── user.proto
│   ├── friend.proto
│   └── rpc_header.proto
├── examples/
│   ├── provider/
│   └── consumer/
├── test/
└── CMakeLists.txt
```

这个结构会让你简历和面试看起来更专业。

---

# 七、每个模块要准备的面试点

## 协议设计

面试可能问：

* 为什么需要自定义协议头
* 怎么解决粘包拆包
* 为什么要有 request_id
* 怎么区分请求和响应

你要会答：

* TCP 是字节流，没有消息边界
* body_len 用来确定完整包长度
* request_id 用来匹配请求和响应，方便以后支持异步化

---

## Protobuf

面试可能问：

* 为什么选 Protobuf
* 比 JSON 好在哪
* service/method 怎么映射

你要会答：

* 序列化体积更小
* 编解码更快
* schema 明确，跨语言扩展性更好

---

## 并发模型

面试可能问：

* 为什么引入线程池
* 线程池大小怎么定
* 网络线程和业务线程怎么分工

你要会答：

* 避免每请求一线程带来的创建销毁开销
* 控制并发度，提升稳定性
* 网络线程只处理收发和分发，业务线程负责方法执行

---

## 注册发现

面试可能问：

* 为什么要注册中心
* 服务地址怎么管理
* 多实例怎么选节点

你要会答：

* 解耦服务提供方和调用方
* 服务地址动态维护，避免写死
* 简单场景可用随机或轮询做负载均衡

---

## 超时与容错

面试可能问：

* 服务端挂了怎么办
* 客户端调用超时怎么办
* 连接复用怎么考虑

你要会答：

* 客户端设置超时，失败快速返回
* 错误码+日志便于定位问题
* 长连接可减少频繁建连成本，后续可扩展连接池

---

# 八、简历怎么写

等你做完后，项目描述可以往这个方向写：

**C++ 轻量级 RPC 框架**

* 基于 C++、TCP/epoll 和 Protobuf 实现轻量级 RPC 框架，支持服务发布、远程调用、请求分发与响应返回
* 设计自定义通信协议，定义消息头与消息体格式，通过长度字段解决 TCP 粘包拆包问题
* 服务端维护 service/method 映射关系，完成请求反序列化、动态路由调用与响应序列化回传
* 基于线程池处理并发请求，降低业务执行阻塞网络线程的影响，提升服务端并发处理能力
* 基于 Redis 实现简单服务注册与发现机制，支持客户端按服务名动态寻址，并通过随机/轮询策略进行负载均衡
* 增加日志、错误码、超时控制等机制，并编写 benchmark 程序测试吞吐与平均延迟

---

# 九、项目难点建议你重点做这几个

别什么都做，重点打磨 3 个难点最划算：

## 难点 1：协议设计与拆包

这是最基础也最容易被问的。

## 难点 2：服务注册 + 方法动态调用

这是“框架味”最强的部分。

## 难点 3：线程池 + 并发处理

这是和 C++ 后端岗位最相关的部分。

把这三个讲透，已经很够用了。

---

# 十、4 周执行版计划

## 第 1 周

* 学 protobuf 基本用法
* 定义 user.proto
* 实现最简单 TCP client/server
* 打通一次 Login RPC

## 第 2 周

* 完成协议封装
* 完成 service/method 动态分发
* 支持多个接口
* 加日志和配置文件

## 第 3 周

* 引入线程池
* 改成并发处理
* 接入 Redis 做注册发现
* 做一个简单负载均衡

## 第 4 周

* 加超时、错误码
* 写 benchmark client
* 压测
* 画架构图
* 整理 README 和简历描述

---

# 十一、你现在别做的东西

为了保证项目能落地，这些先别碰：

* ZooKeeper
* 真正复杂的异步 Future 框架
* 完整协程调度器
* 自己从零实现 Protobuf 替代品
* 分布式一致性协议

这些都很花时间，而且对校招收益不高。

---

# 十二、最终建议

你的目标不是“做一个业内最强 RPC 框架”，而是：

**做一个你自己真正实现过、能跑、能压测、能讲清楚的 RPC 项目。**

这才是最适合校招的。

下一步最有效的是：我直接帮你继续拆成一版
**“RPC 项目的详细功能清单 + 每周任务 + 简历表述模板”**。
